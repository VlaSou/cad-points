# cad-points
LT-compatible AutoLISP bundle for exporting point coordinates from selected layers, numbering points, sampling curved geometry, inserting a configurable point table into the DWG, and optionally generating approximate contour curves from Z coordinates.
